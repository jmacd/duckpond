{
  "name": "noyo-harbor-notebooks",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "preview": "notebooks preview --root export",
    "build": "notebooks build --root export -- export/*.html",
    "clean": "rm -rf .observable/dist .observable/cache"
  },
  "dependencies": {
    "@observablehq/notebook-kit": "^1.5.1"
  },
  "devDependencies": {
    "puppeteer": "^24.36.1"
  },
  "engines": {
    "node": ">=22"
  }
}
