# Septic site config
#
# Copy this to deploy.env and fill in your values:
#   cp deploy.env.example deploy.env
#
# deploy.env is gitignored — credentials stay local.

# Remote data source (for rsync)
DEPLOY_HOST=debian@septicplaystation.casparwater.us
DEPLOY_DATA_DIR=/home/data

# S3 backup (dev MinIO — change for production)
S3_URL=s3://septic-dev
S3_ENDPOINT=http://watershop.casparwater.us:9000
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_ALLOW_HTTP=true
